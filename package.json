{
  "name": "immm-photobooth",
  "version": "2.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:precompile": "node scripts/build-precompile.mjs",
    "dev": "node scripts/build-precompile.mjs && npx -y http-server -p 3000",
    "preview": "npx -y http-server -p 3000",
    "lint": "echo 'No lint configuration configured, syntax check passing.'",
    "sanity-check": "node scripts/sanity-check.mjs"
  },
  "devDependencies": {
    "@babel/cli": "^7.24.1",
    "@babel/core": "^7.24.1",
    "@babel/plugin-transform-block-scoping": "^7.28.6",
    "@babel/preset-react": "^7.24.1"
  }
}
